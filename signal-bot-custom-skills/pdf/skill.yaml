name: pdf
description: >
  Read and extract text only from genuine PDF documents. Never use this skill
  to inspect audio, images, arbitrary files, or to probe whether a path exists.
version: "1.0.0"
enabled: true
command: /pdf
command_arg: url
command_usage: "/pdf <url or path>"

tools:
  - "pdf_skill:read_pdf"
